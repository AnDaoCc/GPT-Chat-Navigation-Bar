# 更新日志

## 2026-V8.4新版ChatGPT全功能兼容版 / 2026.8.4 - 2026-07-12

- 兼容新版 ChatGPT 的消息角色与轮次结构，同时支持 `data-message-author-role`、`data-author-role`、`data-author`、`data-role`、`data-turn`、`data-turn-id` 和 `data-message-id`。
- 导航索引、Token 统计、文档导出、素材提取、正文排版、宽度调节、消息跳转和流式更新监听统一使用新的消息语义兼容层。
- 兼容新版 `main#main`、`model-switcher-dropdown-button` 和双层输入组件，避免将 `#prompt-textarea.ProseMirror` 误识别为画布内容。
- 更新远程 DOM 兼容规则，扩大新版 conversation turn 选择器覆盖范围。
- 增加新版角色标记、嵌套消息标记和输入区排除的自动化回归测试。

## 2026-V8.3索引精简与Token隔离版 / 2026.8.3 - 2026-06-28

- 删除聊天页面和导航索引的持久化缓存、恢复逻辑及弹窗缓存管理界面。
- 升级时自动清理扩展存储和 ChatGPT 页面 `localStorage` 中经过结构校验的旧索引。
- Token 和结构大纲改为只使用当前标签页的临时内存数据，关闭标签页后自动释放。
- 为每个标签页和会话建立独立代次，切换聊天、新建窗口或打开临时 GPT 页面时立即清空旧统计。
- 旧会话的延迟扫描和 Token 批次结果不再触发新会话统计更新。
- 保留插件设置、收藏素材、模型目录、兼容规则以及复制和导出偏好。
- 增加旧索引识别、设置迁移、会话身份和 Token 响应隔离测试。

## 2026-V8.2画布宽度修复版 / 2026.8.2 - 2026-06-20

- 修复 `2026.8.1` 中画布宽度设置已保存、但页面视觉宽度不变化的问题。
- 宽度启用标记改为明确写入 `"true"`，恢复与 CSS 精确选择器和会话恢复逻辑的匹配。
- 恢复官方宽度或关闭宽度布局时直接移除激活标记。
- 保留固定画布会话、幂等宽度写入、滚动位置恢复和代码画布原生字体策略。
- 增加宽度标记启用、CSS 选择器命中与关闭清理回归测试。

## 2026-V8.1画布滚动修复版 / 2026.8.1 - 2026-06-20

- 修复调整画布宽度或字号后，长画布滚动到中间会回弹、无法到达底部的问题。
- 画布打开后绑定固定布局会话，内部段落懒加载和虚拟代码行变化不再重新选择宽度目标。
- 宽度改为应用到非滚动外层容器，不再使用持续计算的横向 `transform`。
- 画布宽度只在设置应用、窗口尺寸变化或画布整体替换时写入，相同数值不重复写入。
- 应用文档画布字号或宽度时保存并恢复滚动位置，接近底部时继续保持底部锚定。
- 虚拟化代码画布保留原生字号和行高度量，优先保证滚动稳定；宽度调节继续生效。
- 增加画布会话、幂等宽度写入、根节点替换、代码画布识别及滚动恢复测试。

## 2026-V8性能优化版 / 2026.8.0 - 2026-06-19

- 正文与画布宽度竖条改为轻量轮廓预览，松手后一次应用和保存。
- 弹窗字号、字距、行距和宽度改为本地草稿预览，结束调节后才写入设置。
- 显示设置更新不再重新读取全部聊天缓存。
- Token 精确计数迁移到后台批处理，内容脚本从约 2.8MB 降至约 275KB。
- 移除页面稳定状态下的周期轮询，收敛画布、表格和代码块观察逻辑。
- 兼容规则默认每 24 小时空闲同步，并优先保留健康的内置规则。
- 增加 Chrome 114 最低版本声明和 Chrome/Edge 共用的 Manifest V3 构建。
- 增加兼容规则、画布识别、Token 批处理与降级估算测试。

## 2026-V7正式版 / 2026.7.0 - 2026-06-05

- 更新内置版本标识为 `2026-V7正式版`。

## 2026-V6 / 2026.6.0 - 2026-05-19

这是一次面向长对话、宽表格、代码块和本地缓存管理的大版本增强。

### 新增

- 新增导航节点智能分组：按本地规则归类为需求讨论、代码修改、报错排查、表格/数据、总结整理、常规对话。
- 新增分组折叠视图：组头显示节点数量、总 token 和热度信息，搜索或收藏筛选时只显示命中的分组。
- 新增节点重命名和备注：自定义标题优先显示，原始摘要仍可用于搜索和恢复默认。
- 新增表格增强工具：复制整表、当前行、当前列、选中区域，支持下载 CSV。
- 新增表格编号和跳转：工具条显示 `表 1/3`，可跳到上一张或下一张表格。
- 新增代码块工具条：复制文件名、复制为 Markdown、下载文件、折叠或展开长代码。
- 新增 diff 代码块高亮：仅对 diff 代码块高亮增加行和删除行。
- 新增 Token 消息明细：显示最耗 token 的前 5 条消息或节点，并支持点击跳转。
- 新增 token 预算预警：超过 82% 黄色提示，超过 100% 红色提示。
- 新增正文宽度调节开关：放在左下角，减少误触和界面重叠。
- 新增弹窗后台设置分类折叠：阅读显示、画布文字、行为、Token、缓存位置、缓存聊天、缓存位置说明、兼容规则按需展开。
- 新增按聊天文档精确清理缓存：可选择具体聊天缓存删除，也保留一键清理能力。
- 新增缓存位置说明：标注哪些缓存可以删除，哪些配置建议保留。
- 仓库继续忽略 `release/` 本地打包产物，避免大文件影响后续开发。

### 优化

- 表格复制按钮改为右侧竖向工具条，并尽量避开正文、宽度调节线、官方按钮和画布按钮。
- 设置面板从页面右侧导航栏移到插件弹窗后台，让页面内导航栏更清爽。
- 数值滑块增加精准数值输入框，方便直接输入字号、行距、字距和正文宽度。
- 字体、行距、字距、正文宽度调整改为 CSS 变量预览，停止拖动后再保存。
- 正文宽度拖拽使用 requestAnimationFrame 合批，减少长页面拖动时的卡顿。
- 设置保存采用防抖策略，避免滑块每次变化都写入 `chrome.storage`。
- 宽表格、多个表格和画布模式下的按钮避让逻辑进一步收敛。
- 弹窗后台布局更紧凑，默认不再一眼铺满所有设置。

### 修复

- 修复新标签页仍显示上一个标签页聊天导航气泡的问题。
- 修复某些情况下 GPT 回答被误收录为导航节点的问题。
- 修复清理缓存后重新进入已有聊天时，导航节点顺序按刷新先后混乱的问题。
- 修复 OpenAI 模型文档跳转到 `developers.openai.com` 后直接从内容脚本请求导致的 CORS 错误，相关公开元数据请求改由后台代理处理。
- 修复表格工具、上下跳转按钮、画布按钮和宽度调节线互相重叠的问题。
- 修复表格复制按钮在部分宽表格场景下消失或压住文字的问题。

### 隐私与缓存

- 所有新增功能继续只使用本地 DOM 和本地缓存。
- 不接入外部 AI，不上传聊天内容。
- 新增缓存字段均为 optional，旧缓存可继续读取。
- `release/` 保持为打包产物目录，不作为源码依赖。

### 验证

- 已执行类型检查：`npm run typecheck`
- 已执行构建：`npm run build`
- 已执行打包：`npm run package`
- 已执行空白字符检查：`git diff --check`
